{
  "name": "@ninelives/site",
  "version": "0.1.0",
  "description": "Nine Lives Co. — the public site (coming-soon to start), styled entirely by @ninelives/necromancy.",
  "private": true,
  "type": "module",
  "dependencies": {
    "@ninelives/necromancy": "1.0.0"
  },
  "scripts": {
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "build:sass": "sass --no-source-map --quiet-deps src/styles.scss .tmp/necromancy.css",
    "build:css": "npm run build:sass && tailwindcss -i src/input.css -o dist/css/main.css --minify",
    "build:html": "node build.mjs",
    "build": "npm run build:html && npm run build:css",
    "serve": "http-server dist -o -c-1 -p 8097",
    "start": "node dev.mjs"
  }
}
